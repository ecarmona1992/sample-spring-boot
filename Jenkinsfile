pipeline{
    agent any
    
    stages{
        stage("Build"){
            steps{
                echo "starting the build step"
            }
        }

        stage("Test"){
            steps{
                echo "Starting tests"
            }
        }

        stage("Deploy"){
            steps{
                echo "Starting the deploy"
            }
        }
    }
}